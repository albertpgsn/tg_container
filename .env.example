# Local development only. Production uses .env.docker plus Docker secrets.
TELEGRAM_API_ID=123456
TELEGRAM_API_HASH=replace_me
SESSION_ENCRYPTION_KEY=replace_with_a_fernet_key
ADMIN_BOOTSTRAP_USERNAME=admin
# Generate with: python -c "from argon2 import PasswordHasher; print(PasswordHasher().hash('your-long-password'))"
ADMIN_BOOTSTRAP_PASSWORD_HASH=replace_with_an_argon2id_hash
COOKIE_SECURE=false
DATABASE_URL=sqlite:///./data/mvp.db
